export enum ClassifyTab {
  CLASSIFY = "classify",
  EXPLORE = "explore",
}

export enum AboutPage {
  BUNDLE = "bundle",
  IMPORTER = "importer",
  CLASSIFIER = "classifier",
  PARTNER = "partner",
}
