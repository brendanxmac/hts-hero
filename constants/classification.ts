export const NUM_FREE_CLASSIFICATIONS = 5

/** Max classifications per billing cycle for Starter plan subscribers. */
export const STARTER_MONTHLY_CLASSIFICATION_LIMIT = 10

/** Max anonymous classifications per browser token (cookie). */
export const MAX_ANONYMOUS_CLASSIFICATIONS_PER_TOKEN = 1
